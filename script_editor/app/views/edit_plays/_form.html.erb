<%= form_for(edit_play) do |f| %>
  <% if edit_play.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(edit_play.errors.count, "error") %> prohibited this edit_play from being saved:</h2>

      <ul>
      <% edit_play.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :play %>
    <%= f.text_field :play %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

